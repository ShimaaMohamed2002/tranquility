import 'package:flutter/material.dart';

class EditTitleSheet extends StatelessWidget {
  const EditTitleSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SimpleDialog();
  }
}
