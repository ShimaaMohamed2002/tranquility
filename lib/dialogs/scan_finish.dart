import 'package:flutter/material.dart';

class ScanFinishSheet extends StatelessWidget {
  const ScanFinishSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SimpleDialog();
  }
}
